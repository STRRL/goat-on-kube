apiVersion: v1
kind: Service
metadata:
  name: {{ include "goat-exporter.fullname" . }}
  labels:
    {{- include "goat-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goat-exporter.selectorLabels" . | nindent 4 }}
